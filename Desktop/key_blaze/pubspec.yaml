name: key_blaze
description: "A local typing game — KeyBlaze: Type. Blaze. Rise."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Icons and local storage packages
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.3

  # Animations and confetti
  animated_text_kit: ^4.2.2
  confetti: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
